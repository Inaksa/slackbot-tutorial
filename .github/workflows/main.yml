on:
  issues:
    types:
      - opened

jobs:
  label_issue:
    runs-on: ubuntu-latest
    steps:
      - env:
          GH_TOKEN: ${{ secrets.MY_TOKEN }}
          ISSUE_URL: ${{ github.event.issue.html_url }}
        run: |
          curl -X POST -H 'Authorization: Bearer xoxb-7338284001654-7651806241538-UGeZfZLn0eLMHVcSp0fiWcLP' -H 'Content-type: application/json;charset=utf-8' --data '{"channel":"#testplugins","text":"Issue: $ISSUE_URL"}' https://slack.com/api/chat.postMessage
